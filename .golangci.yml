version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused
    - asciicheck
    - bodyclose
    - dogsled
    - dupl
    - copyloopvar
    - gocritic
    - revive
    - misspell
    - prealloc
    - unconvert
    - whitespace
  exclusions:
    paths:
      - plz-out/
      - tests/
    rules:
      - text: SA1019  # Upstream packages seem not to be ready to update protobuf yet
        linters: [staticcheck]
      - text: SA4006  # Too many false positives :(
        linters: [staticcheck]
      - text: SA5008  # Warns for duplicate struct tags which are meaningful to go-flags
        linters: [staticcheck]
      - text: QF1001|QF1003|ST1005  # Keep existing style debt suppressed for now.
        linters: [staticcheck]
      - text: unslice  # This may be useful in some places.
        linters: [gocritic]
      - text: ifElseChain  # Generally don't agree.
        linters: [gocritic]
      - text: indent-error-flow # Similar to above, seems to be making questionable choices
        linters: [gocritic]
      - text: appendAssign
        linters: [gocritic]
      - text: halp  # Make misspell be quiet about this.
        linters: [misspell]
      - text: exitAfterDefer # Potentially useful but not in any cases it fires right now.
        linters: [gocritic]
      - text: unused-parameter # Quite a few of these, some legit, many are fulfilling interfaces
        linters: [gocritic]
      - text: redefines-builtin # Not really a big issue
        linters: [gocritic]
      - text: empty-block # Only came up once and was a false positive. This should be easily handled by review.
        linters: [gocritic]
      - text: exported|unused-parameter|redefines-builtin-id|empty-block|indent-error-flow|package-comments
        linters: [revive]
      - text: Consider preallocating
        linters: [prealloc]
      - text: unnecessary conversion
        linters: [unconvert]
      - path: _test\.go
        linters:
          - errcheck
          - dupl

formatters:
  enable:
    - gofmt
  exclusions:
    paths:
      - plz-out/
      - tests/
